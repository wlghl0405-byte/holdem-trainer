# 육지덤 버전 기록

형식: 시맨틱 버전(MAJOR.MINOR.PATCH). 날짜는 배포(푸시) 기준. 각 버전은 git 태그(`v1.1.0` 등)와 일치한다.

## v1.1.3 (2026-09-10)

- 좌석 앞 베팅 칩 배지를 제거했다. 금액은 플레이트의 액션 띠(벳 200 · 콜 200 · SB 50)에 이미 표시되며, 폰에서 보드 카드와 겹치던 문제가 사라진다.

## v1.1.2 (2026-09-10)

좌석 구성 개선.

### 화면 구성
- 좌석 플레이트를 이름 · 칩 · 액션(콜/레이즈/올인 띠) 한 덩어리로 묶고, 베팅 금액은 좌석 앞(테이블 중앙 쪽)에 칩 배지로 놓는다. 좌석 아래로 배지가 겹겹이 매달리던 구조를 없애 8명이어도 서로 겹치지 않는다.
- 좌석은 다시 펠트 안쪽 타원에 배치(5명 이상은 각도표, 위아래 간격 확보). 폰에서는 바깥 좌석을 조금 올리고 보드를 조금 내려 베팅 칩이 보드 카드에 가리지 않게 했다.
- 서체는 시스템 기본 고딕으로 되돌리고, 내 자리 박스의 빛나는 테두리와 펠트 상단 하이라이트를 제거했다. 버튼 크기·굵기는 v1.0 기준으로 복원.

## v1.1.1 (2026-09-10)

v1.1.0 배포 직후 받은 화면 피드백을 반영한 정돈 버전.

### 화면 구성
- 상태 문구(내 차례 시계, 콜 필요 금액, 대기·파산 안내)를 테이블 중앙 팟 아래 두 줄 고정 영역으로 옮겼다. 내 자리 박스는 카드와 이름·칩·족보만 남기고 높이를 고정했다.
- 자리 비움 · AI에게 맡기기 · 돌아오기는 헤더의 자리 비움 버튼을 누르면 팝업으로 고른다(온라인에서만 표시).
- 액션 버튼 영역의 높이를 고정해 버튼 수가 달라져도 테이블이 움직이지 않는다.
- 상대 베팅 금액을 어두운 알약 배지로 바꿔 또렷하게 보이게 했다. 폰에서 보드 카드와 겹치지 않도록 좌석을 조금 위로 올렸다.
- 폰 헤더를 한 줄로 정리(방 코드 · 난이도만 표시). 채팅 독은 최근 2줄.
- 안내 문구를 모두 두 줄 안에 들어오게 줄였다.
- 서체는 시스템 기본 고딕 유지.

### 검증
- PC(상대 3·5명)와 폰 폭 390px(상대 3·8명)에서 내 차례, 상대 차례, 자리 비움 팝업, 쇼다운 결과 화면을 확인. npm test 전부 통과(온라인 47건).

## v1.1.0 (2026-09-10)

앱 이름을 **육지덤**으로 바꾸고, 온라인 멀티의 접속 안정성과 화면 구성을 전면 정비한 버전.

### 명칭·설정
- 앱 이름 육지덤(제목, 헤더, 홈화면 앱 이름, Electron 창 제목).
- 난이도 표기 통일: 하수 · 중수 · 고수 · 프로 · 섞기.
- 차례 제한 시간 선택지: 3초 · 5초 · 10초 · 무제한(기본 10초).
- 좌석의 봇 성향 라벨(공격형·수비형 등) 제거. 헤더에 방 코드와 난이도만 표시.

### 접속·자리 규칙
- 접속이 끊겨도 자리를 잃지 않는다. 끊기면 자동으로 자리 비움이 되고, 돌아오면 같은 자리로 복귀하며 자리 비움도 자동 해제된다(기존 45초 뒤 자동 퇴장 폐지).
- 화면을 떠나면(다른 앱, 화면 잠금) 3초 뒤 자리 비움. 화면으로 돌아오면 연결 상태를 확인(ping/pong)해 필요 시 즉시 재연결.
- 저장된 토큰이 없어도 같은 이름의 오프라인 자리는 되찾을 수 있다(접속 중인 자리는 이름 중복 거부).
- 방장은 접속 중인 첫 사람. 방장이 잠시 끊기면 다음 사람이 맡고, 돌아오면 원래 방장에게 돌아온다.
- 사람이 아무도 칠 수 없으면(모두 자리 비움) 봇끼리 돌리지 않고 멈춘다.
- 사람이 모두 끊긴 방은 30분 뒤에 정리된다.
- 서버가 재시작되어 방이 사라진 경우 안내 후 새 방을 만들 수 있게 정리한다.
- Render 무료 서버가 게임 도중 잠들지 않도록, 사람이 있는 방이 있을 때 5분마다 서버가 자기 주소를 호출한다.

### 파산·부활
- 봇은 파산하면 테이블을 떠난다.
- 사람은 영영 파산하지 않는다. 처음 파산 시 시작 칩의 50%, 그 뒤로는 파산할 때마다 10%(최소 1BB)로 다음 핸드에 다시 시작한다.
- 파산 시 결과 카드에 파산 블록(다음 핸드 시작 금액)을 표시하고, 부활 시 큰 알림과 칩 연출, 채팅 알림 줄로 알린다. 내 이름 옆에 부활 횟수 표시.

### 연출
- 카드 뒤집기(보드·내 패·쇼다운 공개), 내 차례 펄스, 베팅 배지 팝, 팟 금액 튐, 올인 섬광, 승자 후광, 팟에서 승자 자리로 칩 날아가기, WIN 색종이.
- 올인 쇼다운(남은 보드가 있을 때): 조명을 낮추고 천천히 공개하며 마지막 카드 전에 두구두구 연출.
- 같은 핸드 안에서는 이미 보여준 카드·배지를 다시 애니메이션하지 않는다.

### 화면 구성
- 상대 좌석을 레일 위에 배치. 5명 이상은 각도표로 양옆 아래까지 넓게 펴서 보드(가운데 카드)와 겹치지 않게 했다. 보드가 항상 좌석보다 위에 그려진다.
- 폰 하단 탭바 제거. 족보와 어드바이저는 헤더 버튼을 누르면 팝업으로 뜬다.
- 채팅 독: 최근 3줄 + 이모지 12개 한 줄(👍 👏 😄 😎 🤑 🔥 🤔 👀 ⏳ 😱 😭 💀).
- 안내 문구(차례 시계, 콜 필요 금액 등)를 내 자리 박스 오른쪽으로 옮겨 세로 공간을 차지하지 않게 했다. 차례 시계는 알약 배지(3초 이하 강조).
- 스테이지 알림(턴·리버)의 카드를 미니 카드 형태로 표시.
- 서체는 Pretendard 유지.

### 버그 수정
- 로비(테이블 전)에서 방장이 잠깐 끊기면 방장 자격을 잃던 문제.
- 게임 재시작 후 첫 핸드의 쇼다운 연출이 나오지 않던 문제(핸드 번호가 1부터 다시 시작해 연출 키가 겹침).

### 검증
- `npm test`: 엔진 54 · 게임 흐름 17 · 클라이언트 · 봇 4 · 온라인 47건 통과.
- 온라인 테스트에 자리 비움 규칙, 방장 규칙, 부활 금액, 봇 파산 퇴장, ping/pong 검증 추가.

## v1.0.1 (2026-09-09)

온라인 멀티플레이 추가.

- 진행 로직을 `table.js`로 분리해 브라우저와 서버가 공유. Node + ws 서버(`server.js`)가 덱과 진행을 쥐고 각 접속자에게 자기 좌석 기준 화면만 보낸다.
- 방 코드(숫자 4자리)와 초대 링크(`?room=CODE`), 토큰 재접속, 봇 추가, 진행 중 입장은 다음 핸드부터, 차례 제한 시간, 방장 이양, 최대 8명.
- 베팅은 버튼(폴드 · 체크/콜 · 삥/따당 · 쿼터 · 하프 · 풀 · 올인).
- 봇 난이도 5단계(섞기는 봇마다 성향 다름), 자리 비움 · AI 대리, 채팅과 이모지 말풍선, 쇼다운 연출(보드 한 장씩, 약한 패부터 공개).
- Render 무료 서비스 배포(`render.yaml`), PWA(홈화면 추가), README 절차.
- 검증: 실소켓 온라인 테스트 · 봇 대결 테스트 · 가짜 DOM 클라이언트 테스트 추가.

## v1.0.0 (2026-08-12)

첫 버전. 홀덤 룰과 족보를 익히기 위한 혼자 연습용 로컬 게임.

- 족보 판정 · 몬테카를로 승률 · 아웃츠 · 팟오즈 엔진(`engine.js`), 정식 룰(블라인드, 버튼 로테이션, 최소 레이즈, 사이드팟).
- 상대 1~8명, 난이도 3단계, 어드바이저(현재 족보 · 승률 · 아웃츠 · 콜 가치 · 완성 가능 족보 분포), 족보표 상시 노출.
- 핸드 종료 결과 카드(승자와 내 패 비교), 진행 속도 조절, 액션 배지와 알림 배너.
- 모바일 반응형 레이아웃, GitHub Pages 호스팅, 단일 파일 배포본(`build-portable.js`), Electron 실행.
- 검증: 엔진 테스트 · 가짜 DOM 자동 플레이 테스트(칩 총량 보존).
